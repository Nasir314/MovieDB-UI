/*
 * ViewMore Messages
 *
 * This contains all the text for the ViewMore container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ViewMore';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ViewMore container!',
  },
});
