import styled from 'styled-components';
import { BlackColor } from '../../utils/constants';

const H2 = styled.h2`
  font-size: 16px;
  color: ${BlackColor};
  font-weight: 500;

  /* margin-bottom: 0.25em; */
  /* background: #414C5D; */
`;

export default H2;
